{
  "name": "vlive-docs-server",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny release deploy dashboard for vlive-docs.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && bash -n scripts/deploy.sh"
  },
  "engines": {
    "node": ">=18"
  }
}
